import LiveGraphComponent from "../components/LiveGraph";

export default function ModelBuild() {

    return (
        <div>
            <h1>Model Build Page</h1>
            <LiveGraphComponent/>
        </div>
    );
}